// Note: ensure to only import TabbedContent and TabHeight.
// Other components (e.g. Tab, TabList, and TabPanel)
// should not be consumed independently.
// They are consumed and managed internally by TabbedContent

export { default as TabbedContent } from './TabbedContent'
export { TabHeight } from './Tab'
